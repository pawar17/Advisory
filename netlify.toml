# Netlify config for XPense frontend (Vite/React SPA)

[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA: serve index.html for all routes so client-side router works
[[redirects]]
  from    = "/*"
  to      = "/index.html"
  status  = 200
